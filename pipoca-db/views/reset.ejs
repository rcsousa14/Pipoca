<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/style.css" type="text/css" />
    <title>Password reset</title>
    <script defer src="/scripts/password_reset.js"></script>
  </head>
  <body class="bg-gradient-to-l from-red to-orange">
    <div
      class="flex flex-col flex-wrap content-center items-center mx-auto py-36"
    >
      <img
        width="80"
        src="/images/white.png"
        alt="logo"
        title="logo"
        class="py-1"
      />
      <h3 class="text-white font-bold pb-2 text-2xl"><%= title %> </h3>
      <form
        id="form"
        action="reset-password"
        method="post"
        class="bg-white md:w-1/3 lg:w-1/4 w-1/2 shadow-md rounded p-4"
      >
        
        <% if(msg.length > 0 && msg !=null){ %> 
          <p class="text-lg text-center"> <strong><%= msg %></strong></p>
          <% } else { %> 
        <input type="hidden" name="token" value="<%- token %>" />
        <span id="passError" class="text-sm text-red py-1"></span>
        <label class="block py-1" for="password">
          <span class="text-gray-700">Senha</span>
          <input
            type="password"
            name="password"
            id="password"
            class="form-input mt-1 block w-full border rounded"
            placeholder="************"
            required
          />
          
        </label>
        <label class="block py-1" for="confirmation">
          <span class="text-gray-700">Confirmação</span>
          <input
            type="password"
            name="confirmation"
            id="confirmation"
            class="form-input mt-1 block w-full border rounded"
            placeholder="************"
            required
          />
          
        </label>

        <button
          type="submit"
          class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 my-4 rounded w-full"
        >
          submit
        </button>
       <% } %> 
      </form>
    </div>
  </body>
</html>
